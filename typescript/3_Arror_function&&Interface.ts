let log = function(message){
    console.log(message);
}

let doLog = (message) => console.log();

interface Point{
    x: number,
    y: number,
    draw: () => void
}

let drawPoint = (point: Point) => {

}

